export enum EventStatus {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
    CANCELLED = "canceled"
}

export enum EventParticipationStatus {
    ATTENDING = "attending",
    NOT_ATTENDING = "not attending",
}
  